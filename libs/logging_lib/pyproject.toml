[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "logging-lib"
version = "0.2.0"
description = "Shared structured logging utilities for microservices with Syslog-compatible console logging"
readme = "README.md"
authors = [
    { name = "ElVirtualJefe", email = "info@virtualjefe.com" }
]
license = { text = "MIT" }
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Topic :: System :: Logging"
]
requires-python = ">=3.10"
debendencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
    "mypy>=1.10"
]

[tool.hatch.build.targets.wheel]
packages = ["src/logging_lib"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py310"
