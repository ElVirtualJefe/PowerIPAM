[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "constants-lib"
version = "0.1.0"
description = "Shared structured constants library for microservices"
readme = "README.md"
requires-python = ">=3.14"
debendencies = []

[tool.hatch.build.targets.wheel]
packages = ["src"]
